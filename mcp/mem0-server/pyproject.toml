[project]
name = "mem0-mcp-server"
version = "0.1.0"
description = "MCP server for shared semantic memory via Mem0 (Qdrant + Ollama)"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.0.0",
    "mem0ai>=0.1.0",
    "ollama>=0.4",
]

[tool.hatch.build.targets.wheel]
packages = ["."]
